[project]
name = "example-app"
version = "1.0.0"
description = "An example app using uv / PEP 621"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.109.0",
    "pydantic>=1.10.0",
    "uvicorn>=0.20",
]

[project.optional-dependencies]
dev = [
    "pytest~=7.3.0",
    "ruff==0.1.6",
]
